<!DOCTYPE html>
<html>
  <head>
    <title>Weather App v1.11</title>
    <link rel="stylesheet" type="text/css" href="/style.css" />
    <link
      rel="stylesheet"
      href="https://use.fontawesome.com/releases/v5.14.0/css/all.css"
    />
  </head>
  
  <body>
    <div class="container">
      <h1 id="app-title">Smart Weather App</h1>
      <form action="/weather" method="get" id="weather-form">
        <input type="text" name="city" placeholder="Enter city name" id="city-input" />
        <button type="submit" id="get-weather-btn">Get Weather</button>
      </form>

      <% if (error) { %>
        <p class="error" id="weather-error"><%= error %></p>
      <% } else if (weather) { %>
        <div class="card" id="weather-card">
          <!-- Temperature -->
          <p id="weather-temp">
            <i class="fas fa-thermometer-half"></i>
            <%= weather.main.temp %>&deg;C
          </p>

          <!-- Weather Status -->
          <p id="weather-status">
            <i class="fas fa-cloud"></i>
            <%= weather.weather[0].main %>
          </p>

          <!-- City Name -->
          <p id="weather-city">
            <i class="fas fa-map-marker-alt"></i>
            <%= weather.name %>, <%= weather.sys.country %>
          </p>
        </div>
      <% } %>
    </div>
  </body>
</html>
